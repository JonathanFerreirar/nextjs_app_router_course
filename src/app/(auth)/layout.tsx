import React, { ReactNode } from 'react'

const layoutAuth = ({ children }: {children: ReactNode}) => {
  return (
    <div>
      {children}
      LAYOUT DA PAGINA AUTH
      </div>
  )
}

export default layoutAuth